{% if cookieNoticeIsEnabled %}
<div is="flynt-block-cookie-notice" class="flyntComponent boxShadow {{ layout }} {{ theme ?: 'themeReset' }}" role="dialog">
  <div class="container centerMaxWidthContainer">
    <div class="content">
      {{ contentHtml|e('wp_kses_post') }}
    </div>
    <button type="button" class="button button--small" id="cookie-consent-ok-button" aria-label="Close" data-close>{{ closeButtonLabel|e }}</button>
  </div>
</div>
{% endif %}
